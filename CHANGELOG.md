# Changelog

## 4.1

- Renamed to **Squid** (folder, title, and `/squid`). `/flf` and `/ff` still work.
- Save and Enable now keep SexyMap skin, shape, and borders.
- Save and Enable now keep XLoot settings, including AceDB namespaces (Frame, Monitor, Group, and the rest).

## 4.0

- Blizzard now loads SavedVariables. Squid no longer rewrites other addons at login.
- Removed the disk/CMD publish path (`FLF_Setup.cmd`, `!FLF_Data`, offline fallback).
- Named profiles remain: Save addon options, binds, macros, bars, CVars, and Edit Mode, then Enable on a new character or to switch layouts on the same one.
