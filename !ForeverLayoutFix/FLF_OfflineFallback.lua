-- Optional offline fallback. Leave this empty unless you are pasting a
-- SavedVariables dump here on purpose. Setup does not require a dump.
FLF_OfflineFallback = FLF_OfflineFallback or { vars = {} }
